import { PassportData } from '../../types';
export declare function brutforceSignatureAlgorithm(passportData: PassportData): {
    signatureAlgorithm: string;
    hashAlgorithm: any;
    saltLength: number;
};
export declare function verifySignature(passportData: PassportData, signatureAlgorithm: string, hashAlgorithm: string, saltLength?: number): boolean;
