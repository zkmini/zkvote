import { UserIdType } from "./circuits/uuid";
export type Mode = 'register' | 'dsc' | 'vc_and_disclose';
export type EndpointType = 'https' | 'celo' | 'staging_celo' | 'staging_https';
import { Country3LetterCode } from "../constants/countries";
export interface SelfApp {
    appName: string;
    logoBase64: string;
    endpointType: EndpointType;
    endpoint: string;
    header: string;
    scope: string;
    sessionId: string;
    userId: string;
    userIdType: UserIdType;
    devMode: boolean;
    disclosures: SelfAppDisclosureConfig;
}
export interface SelfAppDisclosureConfig {
    issuing_state?: boolean;
    name?: boolean;
    passport_number?: boolean;
    nationality?: boolean;
    date_of_birth?: boolean;
    gender?: boolean;
    expiry_date?: boolean;
    ofac?: boolean;
    excludedCountries?: Country3LetterCode[];
    minimumAge?: number;
}
export declare class SelfAppBuilder {
    private config;
    constructor(config: Partial<SelfApp>);
    build(): SelfApp;
}
export declare function getUniversalLink(selfApp: SelfApp): string;
