import { UserIdType } from '../../../../common/src/utils/circuits/uuid';
import { PublicSignals } from 'snarkjs';
export declare function parseSolidityCalldata<T>(rawCallData: string, _type: T): T;
export declare function getUserIdentifier(publicSignals: PublicSignals, user_identifier_type?: UserIdType): Promise<string>;
